import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
  debugBorder: {
    borderColor: 'red',
    borderWidth: 2,
    borderStyle: 'dotted',
  },
});

export const { debugBorder } = style;
