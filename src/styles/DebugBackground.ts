import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
  debugBackground: {
    borderColor: 'rgba(255,0,0,0.1)',
    borderWidth: 2,
    borderStyle: 'dotted',
  },
});

export const { debugBackground } = style;
